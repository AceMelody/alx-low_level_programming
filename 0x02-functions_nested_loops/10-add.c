#include "main.h"

/**
 * add - adds two numbers
 * @f: the first integer
 * @g: the second integer
 *
 * Return: the sum of @f and @g
 */

int add(int f, int g)
{
	int h;

	h = f + g;
	return (h);
}
